{
  "schemaVersion": 1,
  "kind": "api-package-test-report",
  "issue": 360,
  "contract": "Persist a canonical source checkout in clawhip-owned managed state; explicit update.repo_root wins, operator config is never mutated, and deployment health can use the managed fallback alone.",
  "results": [
    {"surface":"managed sidecar","command":"cargo test source_checkout::tests","status":"passed","evidence":"Fresh canonical round-trip, explicit override, byte-stable operator config, malformed/nonregular/symlink/FIFO rejection, interrupted-write preservation, and invalid checkout rejection passed (7 tests)."},
    {"surface":"deployment health","command":"cargo test repo_root_alone_is_sufficient_for_health_comparison","status":"passed","evidence":"Managed sidecar alone supplies a readable source commit while update.enabled remains false and channel remains absent."}
  ]
}
