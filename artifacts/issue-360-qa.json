{
  "schemaVersion": 1,
  "kind": "api-package-test-report",
  "issue": 360,
  "contract": "Persist canonical update.repo_root only when absent during source install/update, preserving operator config and enabling deployment comparison without auto-update settings.",
  "results": [
    {"surface":"config persistence","command":"cargo test --locked --bin clawhip config::tests::repo_root_","status":"passed","evidence":"Fresh config, comments/unrelated values, explicit value, malformed config, unsafe path, and concurrent explicit-write protection passed."},
    {"surface":"config write serialization","command":"cargo test --locked --bin clawhip config::tests::config_write_lock_excludes_concurrent_writer","status":"passed","evidence":"The stable sibling config lock excludes a concurrent in-tree writer for the full read/backup/commit transaction."},
    {"surface":"final publication no-clobber","command":"cargo test --locked --bin clawhip config::tests::repo_root_write_rejects_replacement_at_final_publication -- --exact","status":"passed","evidence":"A deterministic replacement after final revalidation is moved to a guard, detected by byte comparison, restored, and never overwritten by automatic repo_root persistence."},
    {"surface":"guard-to-link races","command":"cargo test --locked --bin clawhip config::tests::repo_root_write_preserves_target_recreated_before_link -- --exact; cargo test --locked --bin clawhip config::tests::repo_root_write_rejects_fifo_replacement_without_blocking -- --exact; cargo test --locked --bin clawhip config::tests::repo_root_write_publishes_checked_fd_after_temp_name_swap -- --exact","status":"passed","evidence":"A recreated target wins without overwrite, FIFO replacement is rejected via nonblocking nofollow validation, and publication is bound to an unnamed checked file descriptor so a hostile historical temp-name symlink is removed and never published."},
    {"surface":"custom config path","command":"cargo test --locked --bin clawhip config::tests::loaded_config_remembers_non_default_source_path","status":"passed","evidence":"Loaded AppConfig retained its selected non-default path for daemon-approved updates."},
    {"surface":"lifecycle validation","command":"cargo test --locked --bin clawhip lifecycle::tests::update_from_repo_","status":"passed","evidence":"Malformed source checkout paths were rejected before update execution."},
    {"surface":"deployment health","command":"cargo test --locked --bin clawhip deployment::tests::repo_root_alone_is_sufficient_for_health_comparison","status":"passed","evidence":"repo_root alone supplied a readable source commit while enabled remained false and channel remained absent."},
    {"surface":"repository gates","command":"cargo check; cargo clippy -- -D warnings; cargo fmt --all -- --check; cargo test -- --test-threads=1; cargo build --release","status":"passed","evidence":"Full local gates passed after fd-bound atomic publication; serial cargo test passed all 1239 tests."}
  ]
}
